# URL base da API do backend
# Durante o desenvolvimento local, use /api (o Vite fará o proxy)
# VITE_API_URL=/api

# Em produção, configure com a URL completa do seu backend
# Exemplo: VITE_API_URL=https://seu-backend.com/api
